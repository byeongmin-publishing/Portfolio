
(function($) {
    'use strict';

    var $window = $(window),
        $document = $(document),
        $html = $('html'),
        $head = $('head'),
		$screen = $.screen,
        $inArray = $.inArray;

    $(function() {

        $window.on('screen:tablet screen:phone', function(event) {
            
        });
    });
})(jQuery);